# Uygulama ortamı: development / testing / production
FLASK_ENV=development
FLASK_DEBUG=1

# Flask gizli anahtarı (development için örnek)
SECRET_KEY=dev-secret-key-change-me

# JWT için ayrı secret istersen (yoksa SECRET_KEY kullanılır)
JWT_SECRET_KEY=dev-jwt-secret-key-change-me

# PostgreSQL bağlantısı (kendine göre düzenle)
DATABASE_URL=postgresql://devuser:devpass@localhost:5432/marketplace_db

# Test veritabanı (ileride kullanacağız)
TEST_DATABASE_URL=postgresql://devuser:devpass@localhost:5432/test_marketplace_db

# Redis bağlantısı (cache, rate limiting, queue için)
REDIS_URL=redis://localhost:6379/0

# Log seviyesi: DEBUG / INFO / WARNING / ERROR
LOG_LEVEL=DEBUG